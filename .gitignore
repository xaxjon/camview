# binaries are a prerequisite installed by setup.sh, not tracked in git
bin/

# your real camera list (contains credentials) — copy from streams.json.example
streams.json

# generated by gen-config.py
mediamtx.yml

# mediamtx runtime clutter
auto.key
auto.crt

__pycache__/
